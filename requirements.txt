nemoguardrails[sdd,jailbreak]==0.20.0
# [sdd]      = presidio-analyzer, presidio-anonymizer, spacy (Presidio PII)
# [jailbreak] = yara-python (YARA injection detection)
# Presidio NER model: run after pip install: python -m spacy download en_core_web_lg

# Jailbreak heuristics (perplexity via gpt2-large). torch is installed CPU-only in Dockerfile.
transformers
