# Kind cluster config for NeMo Guardrails (PoC)
# Single-node cluster to avoid worker-join issues with Kind + Podman (kubelet unhealthy).
# Usage: kind create cluster --config kind-config.yaml --name guardrails
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
